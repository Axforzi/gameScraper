# Development dependencies (REQ-PKG-4).
#
# Production requirements are always included first, then tooling used only
# during development and in CI (lint, type checks, tests). Do not unpin.
-r requirements.txt

pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.6
mypy==2.3.1
python-dotenv==1.0.1
ipython==9.2.0
