# Production runtime dependencies (REQ-PKG-4).
#
# Direct dependencies only; transitive dependencies resolve via pip.
# Development-only tooling lives in requirements-dev.txt (pytest, ruff, mypy,
# ipython) and MUST NOT appear here. Do not unpin these lines.
Flask==3.1.0
flask-wtf==1.3.0
Scrapy==2.12.0
crochet==2.1.1
Twisted==24.11.0
waitress==3.0.2
nh3==0.3.7
beautifulsoup4==4.13.4
lxml==5.4.0
roman==5.0
cryptography==44.0.3
pyOpenSSL==25.0.0
service-identity==24.2.0
